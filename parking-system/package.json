{
  "name": "parking-system",
  "version": "1.0.0",
  "private": true,
  "description": "Прототип АИС «Автостоянка»: микросервисы auth, clients, parking",
  "workspaces": [
    "common",
    "services/*"
  ],
  "scripts": {
    "test": "LOG_LEVEL=silent JWT_SECRET=test node --test \"tests/unit/*.test.js\"",
    "test:integration": "node --test --test-concurrency=1 \"tests/integration/*.test.mjs\"",
    "demo": "node scripts/demo.mjs",
    "load": "node scripts/load.mjs"
  }
}
